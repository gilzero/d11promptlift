"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const fs_1 = require("fs");
const json_schema_to_typescript_1 = require("json-schema-to-typescript");
async function main(schemas) {
    const schemaDir = (0, path_1.resolve)(__dirname, '..', 'schemas');
    const typesDir = (0, path_1.resolve)(__dirname, '..', 'types');
    return await Promise.all(schemas.map((name) => (0, json_schema_to_typescript_1.compileFromFile)((0, path_1.join)(schemaDir, `${name}.json`), {
        cwd: schemaDir,
        style: {
            singleQuote: true,
        },
    }).then((ts) => (0, fs_1.writeFileSync)((0, path_1.join)(typesDir, `_${name}.d.ts`), ts))));
}
exports.default = main;
main(['system', 'variant', 'emulsifyProjectConfig']);
