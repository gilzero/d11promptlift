"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const simple_git_1 = __importDefault(require("simple-git"));
const fs_1 = require("fs");
const path_1 = require("path");
const getCachedItemPath_1 = __importDefault(require("./getCachedItemPath"));
function cloneIntoCache(bucket, itemPath) {
    return async ({ repository, checkout }) => {
        const destination = (0, getCachedItemPath_1.default)(bucket, itemPath, checkout);
        const parentDir = (0, path_1.dirname)(destination);
        if ((0, fs_1.existsSync)(destination)) {
            return;
        }
        if (!(0, fs_1.existsSync)(parentDir)) {
            await fs_1.promises.mkdir(parentDir, { recursive: true });
        }
        const git = (0, simple_git_1.default)();
        await git.clone(repository, destination, checkout
            ? {
                '--branch': checkout,
            }
            : {});
    };
}
exports.default = cloneIntoCache;
