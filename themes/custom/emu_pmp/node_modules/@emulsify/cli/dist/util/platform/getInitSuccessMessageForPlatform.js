"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = require("chalk");
function getInitSuccessMessageForPlatform(platform, directory) {
    if (platform === 'drupal') {
        return [
            {
                method: 'info',
                message: 'Make sure you install the modules your Emulsify-based theme requires in order to function.',
            },
            {
                method: 'verbose',
                message: `
            - composer require drupal/components
            - composer require drupal/emulsify_twig
            - drush en components emulsify_twig -y
            `,
            },
            {
                method: 'info',
                message: `Once the requirements have been installed, you will need to select a component system to use, as Emulsify does not come with components by default. To do this, navigate to your theme directory (${directory}), and choose a command below.`,
            },
            {
                method: 'verbose',
                message: `
            ${(0, chalk_1.cyan)('List systems')}: emulsify system list
            ${(0, chalk_1.cyan)('Install a system')}: emulsify system install "system-name"
            ${(0, chalk_1.cyan)('Install default system with default components')}: emulsify system install compound
            ${(0, chalk_1.cyan)('Install default system with all components')}: emulsify system install compound --all
            `,
            },
        ];
    }
    return [];
}
exports.default = getInitSuccessMessageForPlatform;
