"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function buildComponentDependencyList(components, name) {
    const rootComponent = components.filter((component) => component.name == name);
    if (rootComponent.length == 0)
        return [];
    let finalList = [name];
    if (rootComponent.length > 0) {
        const list = rootComponent[0].dependency;
        if (list && list.length > 0) {
            list.forEach((componentName) => {
                finalList = [
                    ...new Set(finalList.concat(buildComponentDependencyList(components, componentName))),
                ];
            });
        }
    }
    return finalList;
}
exports.default = buildComponentDependencyList;
