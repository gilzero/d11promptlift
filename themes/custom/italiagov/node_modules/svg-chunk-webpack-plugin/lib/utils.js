import path from 'node:path';
import { fileURLToPath } from 'node:url';
export const PACKAGE_NAME = 'svg-chunk-webpack-plugin';
export const esmResolve = (filePath) => path.resolve(path.dirname(fileURLToPath(import.meta.url)), filePath);
